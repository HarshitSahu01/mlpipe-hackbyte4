[tool.uv]
python = "3.10"

[project]
name = "predict-xplore-backend"
version = "0.1.0"
description = "Predict-Xplore — Stateless ML Inference Compute Gateway"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.111.0",
    "uvicorn[standard]>=0.29.0",
    "celery[redis]>=5.3.6",
    "redis>=5.0.3",
    "docker>=7.1.0",
    "pydantic>=2.7.1",
    "pydantic-settings>=2.2.1",
    "httpx>=0.27.0",
    "python-multipart>=0.0.9",
]
